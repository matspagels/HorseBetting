from collections import namedtuple
import altair as alt
import math
import pandas as pd
import streamlit as st

subheader("Welcome to BetCoin™!")

        write("Create a user or login with your existing account.")
        write("Every new user starts with a blance of 100.")
        write("You can bet on 5 different crypto coins to go up or down within the next 15 seconds.")
        write("Bets entries range between 1 and 10. The return is double or nothing.")
        write("Look on the ranking page how you compare to other players.")
        write("Have fun!")

        subheader("Login or Create a User")
